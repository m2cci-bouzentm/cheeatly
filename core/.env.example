# Cheatly core
# When launched by Electron these are set automatically; override for standalone runs.

PORT=3001
DATABASE_URL=file:../data/cheatly.db

# LLM credentials are passed per-request via X-LLM-Provider / X-LLM-Api-Key
# headers from the Electron app — the server itself holds no API keys.
